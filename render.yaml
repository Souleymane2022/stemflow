services:
  - type: web
    name: stemflow-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        value: sqlite.db
      - key: SESSION_DIR
        value: .
      - key: SESSION_SECRET
        generateValue: true
